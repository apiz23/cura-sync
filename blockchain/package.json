{
  "name": "cura-sync-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Hardhat workspace for CuraSync HealthRecordRegistry on Polygon Amoy",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network amoy",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
